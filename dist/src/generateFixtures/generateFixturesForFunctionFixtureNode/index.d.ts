import { FixtureNode, FunctionFixtureNode } from '../../types';
export declare function generateFixturesForFunctionFixtureNode(
  node: FunctionFixtureNode,
  fixtureNodes: Record<string, FixtureNode>,
  maxFixtures: number
): any[];
